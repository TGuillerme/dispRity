make.model<-function(fun) {
    #This function should test a evolutionary model function and outputs the same function with an extra attribute (to be read by time.series)
    message("Not developed yet!")
}