language: r
warnings_are_errors: false

# blacklist
branches:
  except:
    - master

# whitelist
branches:
  only:
    - release

# Code coverage
r_packages:
  - covr

after_success:
  - bash <(curl -s https://codecov.io/bash)
