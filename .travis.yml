language: r
warnings_are_errors: false

# blacklist
branches:
  except:
    - master

# whitelist
branches:
  only:
    - release
    
 # Devtools install
 r_github_packages:
    hadley/devtools


# Code coverage
r_packages:
  - covr

after_success:
  - Rscript -e 'library(covr); codecov()'
