language: r
sudo: required
warnings_are_errors: false

# blacklist
branches:
  except:
    - master
  only:
    - release
 
# Code coverage
r_packages:
  - covr

after_success:
  - Rscript -e 'library(covr); codecov()'
